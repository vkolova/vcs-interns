// Sum only numbers

// Implement a function called `sum`. It takes two arguments - `a` and `b`.
// `sum` returns the sum of `a` and `b` only if `a` and `b` are numbers! Otherwise it throws a `TypeError`.
