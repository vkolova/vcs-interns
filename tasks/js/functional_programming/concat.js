// Concat only strings

// Implement a function called `concat`. It takes two arguments - `a` and `b`.
// `concat` returns the concatenation of `a` and `b` only if `a` and `b` are stringss! Otherwise it throws a `TypeError`

